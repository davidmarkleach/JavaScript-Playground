import { IfThenPlanner } from "@/components/if-then-planner";

export default function Home() {
  return (
    <main className="flex flex-1 flex-col bg-background">
      <IfThenPlanner />
    </main>
  );
}
